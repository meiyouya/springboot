/*
package com.zql.mybatisplus.service.impl;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.zql.mybatisplus.entity.User;
import com.zql.mybatisplus.mapper.UserMapper;
import com.zql.mybatisplus.service.IUserService;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

*/
/**
 * <p>
 *  服务实现类
 * </p>
 *
 * @author lawliet
 * @since 2019-04-20
 *//*

@Service
public class UserServiceImpl extends ServiceImpl<UserMapper, User> implements IUserService {

    @Override
    @Transactional
    public boolean deleteBatch() {
        baseMapper.deleteById(2);
        System.out.println(1);
        int i = 1/0;
        System.out.println(2);
        baseMapper.deleteById(3);
        return true;
    }
}
*/

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zql.mybatisplus.mapper.db1.ProductMapper">
    <select id="selectById" resultType="com.zql.mybatisplus.entity.db1.Product" parameterType="int">
        select * from t_ms_product where id = #{id}
    </select>
</mapper>
